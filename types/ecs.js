//@flow strict

/*:: import type { AWS_ARN } from './aws'; */

/*:: export type AWS_ECS_Service = {
    status: "ACTIVE",
    taskDefinition: AWS_ARN,
    pendingCount: number,
    loadBalancers: Array<any>,
    desiredCount: number,
    createdAt: number,
    serviceName: string,
    clusterArn: AWS_ARN,
    serviceArn: AWS_ARN,
    deployments: Array< {
        status: "PRIMARY",
        pendingCount: number,
        createdAt: number,
        desiredCount: number,
        taskDefinition: AWS_ARN,
        updatedAt: number,
        id: string,
        runningCount: number,
    }>,
    events: Array<{
        message: string,
        id: string,
        createdAt: number,
    }>,
    runningCount: number,
}; */

